import { Shield } from "lucide-react";
import { ComingSoon } from "@/components/mobile/coming-soon";

export default function TeamsPage() {
  return (
    <ComingSoon
      title="Teams"
      note="A dedicated team directory and roster management view is planned for a later sprint."
      icon={Shield}
    />
  );
}
